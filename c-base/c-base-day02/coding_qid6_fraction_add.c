#include <stdio.h>

int main()
{
    int num1, denom1, num2, denom2;
    scanf("%d / %d + %d / %d", &num1, &denom1, &num2, &denom2);
    printf("%d %d %d %d", num1, denom1, num2, denom2);
    return 0;
}

