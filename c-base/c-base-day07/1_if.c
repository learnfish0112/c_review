#include <stdio.h>

int main()
{
    if(1) {
        if(1)
            printf("if if\n");
        else
            printf("if else cannot see\n");
        printf("if else cannot see?\n");
    }
    return 0;
}

